<?xml version="1.0" encoding="utf-8"?>
<!--<Toolbar xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/toolbar_list"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_alignParentTop="true"
    android:minHeight="?android:attr/actionBarSize"
    android:background="?android:attr/colorPrimary"
    android:title="@string/title_activity_list"
    android:subtitle="@string/subtitle_activity_list"
    android:navigationIcon="@drawable/ic_arrow_back_black_24dp"
    android:titleTextColor="@color/title"
    android:subtitleTextColor="@color/subtitle"
    />-->

<android.support.v7.widget.Toolbar
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/toolbar"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    app:title="@string/title_activity_list"
    app:subtitle="@string/subtitle_activity_list"
    app:titleMarginStart="16dp"
    app:theme="@style/Theme.MyTheme"
    />